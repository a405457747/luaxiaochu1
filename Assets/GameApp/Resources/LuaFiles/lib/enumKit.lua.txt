---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by justi.
--- DateTime: 2024/6/17 10:47
---
local enumKit = {}

local function numberToEnum(enum, num)
    for i, v in pairs(enum) do
        if v == num then
            return i;
        end
    end
    error("don`t have");
end

function enumKit.enumToArray(enum)
    local keys = js.keys(enum);
    table.sort(keys, function(a, b)
        return enum[a] < enum[b];
    end)
    return keys;
end


enumKit.numberToEnum = numberToEnum;
return enumKit;